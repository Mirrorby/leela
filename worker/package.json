{
  "name": "leela-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "echo \"(тесты воркера появятся в этапе 7.3+)\" && exit 0"
  },
  "devDependencies": {
    "wrangler": "^3.78.0",
    "typescript": "^5.5.4",
    "@cloudflare/workers-types": "^4.20240925.0"
  }
}
